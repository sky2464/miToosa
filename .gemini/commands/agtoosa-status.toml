description = "AgToosa: read-only health dashboard — parse Master-Plan, cross-reference git, detect orphans"
prompt = """

## Gemini command routing

This file is the native Gemini CLI command for `/agtoosa-status`. When the user invokes `/agtoosa-status`, execute the AgToosa workflow below — do **not** route to `/create-skill` or generate a project skill for AgToosa workflow names.

Read Docs/AgToosa_Status.md and execute the status workflow. Generated Project Mode — see Docs/AgToosa_Agent.md Operating Contexts (this project's Master-Plan).

Dispatch based on any arguments provided after the command:
- /agtoosa-status          → full status dashboard
- /agtoosa-status plan       → Master-Plan.md health check only
- /agtoosa-status readiness  → initial product readiness gates only (Docs/AgToosa_Readiness.md)
- /agtoosa-status git        → git cross-reference only
- /agtoosa-status orphans    → orphan detection only
- Any other token            → prepend exactly: Note: '<token>' is not a defined sub-command. Did you mean: plan, readiness, git, orphans? Falling back to full dashboard. Then run the full dashboard.

CRITICAL: This is a READ-ONLY command. Do NOT modify any files.

Generate "Recommended Next Actions" via the deterministic algorithm in Docs/AgToosa_Status.md Part 5.5 — priority-ordered, command-grouped, deduped, capped at 5, with 🎯 Quick wins call-out.
"""
