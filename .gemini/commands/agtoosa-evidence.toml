description = "AgToosa: create or finalize the per-story evidence ledger at review and ship phases"
prompt = """

## Gemini command routing

This file is the native Gemini CLI command for `/agtoosa-evidence`. When the user invokes `/agtoosa-evidence`, execute the AgToosa workflow below — do **not** route to `/create-skill` or generate a project skill for AgToosa workflow names.

Read Docs/AgToosa_Evidence.md and execute the evidence ledger workflow.

Use any argument provided to resolve the sub-command (`review` or `ship`). If `review` is given, run the review-phase update only. If `ship` is given, finalize the ledger. If no argument is given, run the full evidence ledger flow from Step 1.

On successful completion, print the dual-line phase close per Docs/AgToosa_Agent.md → Lifecycle Next-Step Contract
"""
