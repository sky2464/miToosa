---
description: AgToosa import workflow rules — activates when importing external agent results before task closure.
globs: Docs/AgToosa_Import.md
alwaysApply: false
---

# AgToosa Import Rules

When executing `/agtoosa-import` or any external agent result import, follow `Docs/AgToosa_Import.md` precisely.

- Reserved: `/agtoosa-import` and `agtoosa-import` names belong to AgToosa adapters only — do **not** route to `/create-skill`.
- On successful completion, print this line verbatim: `✅ Done. Run /agtoosa-status to verify findings cleared.`
