---
description: AgToosa task capture workflow
---

## Cursor command routing

This file is the native Cursor project command for `/agtoosa-task`. When the user invokes `/agtoosa-task`, execute the AgToosa workflow below — do **not** route to `/create-skill` or generate a project skill for AgToosa workflow names.

Read `Docs/AgToosa_Task.md` and execute the `/agtoosa-task` workflow.
