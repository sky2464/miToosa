# flutter-engine-guard

Read-only project specialist. Execute when `lib/core/engine/` changes during build or review.

1. Read `.codex/skills/flutter-engine-guard/SKILL.md` and run its workflow.
2. Return the structured evidence block from `docs/AgToosa_Specialists.md`.
3. Block merge recommendation if forbidden imports found in engines.
