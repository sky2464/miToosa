# hive-schema-guard

Read-only project specialist. Execute when `lib/data/player_progress.dart` or Hive migrations change.

1. Read `.codex/skills/hive-schema-guard/SKILL.md` and run its workflow.
2. Return the structured evidence block from `docs/AgToosa_Specialists.md`.
