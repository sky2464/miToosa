Read @Docs/AgToosa_Update.md and execute the full update workflow.

Arguments provided: $ARGUMENTS

If $ARGUMENTS is provided, treat it as an optional focus hint for the update summary (for example: "show only breaking changes"), but still run the complete workflow.