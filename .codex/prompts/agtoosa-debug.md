## Codex prompt routing

This file is the Codex project prompt for `/agtoosa-debug`. When the user invokes `/agtoosa-debug`, execute the AgToosa workflow below — do **not** route to `/create-skill` or generate a project skill for AgToosa workflow names.

Read `Docs/AgToosa_Debug.md` and execute the `/agtoosa-debug` workflow.
